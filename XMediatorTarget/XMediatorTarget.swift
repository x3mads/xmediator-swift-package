// Intentionally empty.
// This wrapper target exists so the XMediator product can combine the
// XMediator and XMediatorSQLite binary targets and carry their required
// system-framework link settings (binary targets cannot declare either).
